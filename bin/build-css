#!/usr/bin/env bash

set -e

echo "Building CSS..."

. init

if [[ -v optimize ]]; then
  args="--style=compressed --no-source-map"
else
  args=""
fi

sass $args "$project/sass/index.scss" "$dest/index.css"

echo "Done CSS!"
